export const m = 1;

// auto indexed values
enum Color {
  Red,
  Green,
  Blue
}

// custom values
enum Size {
  Big = 'big',
  Normal = 'normal',
  Small = 'small',
}

let red = Color.Red;
let big = Size.Big;

